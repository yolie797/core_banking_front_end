import React from 'react';
import { buildTextStyle } from './TextUtils';

const HPComponent = () => {
  const classes = buildTextStyle({
    fontSize: 'mdText',
    color: 'text-black', // Tailwind class for black color
    fontWeight: 'boldText',
  });

};

export default HPComponent;